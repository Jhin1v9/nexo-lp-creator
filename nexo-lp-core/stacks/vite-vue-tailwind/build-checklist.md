# Build Checklist — Vite + Vue 3 + Tailwind CSS

## Pre-Build
- [ ] All `{{PLACEHOLDER}}` values replaced
- [ ] `npm install` completed
- [ ] No template syntax errors in .vue files

## Build
```bash
npm run build
```

## Verification
- [ ] `dist/` directory created
- [ ] No build errors
- [ ] `npm run preview` renders correctly

## Deploy
Set build: `npm run build`, output: `dist`
