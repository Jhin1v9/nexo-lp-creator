# Build Checklist — Next.js Pages Router

## Pre-Build
- [ ] All `{{PLACEHOLDER}}` values replaced
- [ ] `npm install` completed

## Build
```bash
next build
```

## Verification
- [ ] `dist/` directory created
- [ ] No build errors

## Deploy
Set build: `next build`, output: `dist`
